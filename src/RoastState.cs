namespace Prajire
{
    public enum RoastState
    {
        NotIntialized,
        Initialized,
        Started,
        TurningPoint,
        DryEnd,
        FirstCrack,
        Stopped
    }
}